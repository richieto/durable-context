# Release Doc Notes

Future reference impact when this initiative ships. Do not edit `reference/`
during implementation.

## Shipped product vs maintainer docs

**Product** (what `init` installs): templates, skills, `AGENTS.md` section.

**Not product:** `writing/` — maintainer-only narrative in this monorepo; never
installed. Rebrand updates there are for name consistency, not adopter behavior.

## npm README (not installed)

- Install commands: `npx durable-context init`, `npx reference-docs init`.
- Product names: **Durable Context** and **Reference Docs**.

## Not applicable to reference package adopters

Installed `reference/` tree structure and skills behavior are unchanged except
for branding strings in installed `AGENTS.md` and skill install metadata paths.
