interface:
  display_name: "Project Profile Baseline"
  short_description: "Capture stable repository operating facts"
  default_prompt: "Use $project-profile-baseline to populate stable repository facts from source evidence."
policy:
  allow_implicit_invocation: false
