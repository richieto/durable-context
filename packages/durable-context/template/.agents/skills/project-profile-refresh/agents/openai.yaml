interface:
  display_name: "Project Profile Refresh"
  short_description: "Refresh stable repository operating facts"
  default_prompt: "Use $project-profile-refresh to refresh stable repository facts from current source evidence."
policy:
  allow_implicit_invocation: false
