interface:
  display_name: "Backfill With Context"
  short_description: "Reconstruct durable context from existing work"
  default_prompt: "Use $backfill-with-context to reconstruct a named initiative from the current work."
policy:
  allow_implicit_invocation: false
