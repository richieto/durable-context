interface:
  display_name: "Durable Context Solo"
  short_description: "Continue focused planning across agent sessions"
  default_prompt: "Use $durable-context-solo to continue a named initiative with focused durable planning."
policy:
  allow_implicit_invocation: false
