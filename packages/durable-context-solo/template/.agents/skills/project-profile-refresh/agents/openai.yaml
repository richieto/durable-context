interface:
  display_name: "Project Profile Refresh"
  short_description: "Refresh changed repository capabilities"
  default_prompt: "Use $project-profile-refresh to refresh changed stable facts in the Solo project profile."
policy:
  allow_implicit_invocation: false
